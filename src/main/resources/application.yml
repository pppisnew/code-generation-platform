spring:
  profiles:
    active: local
  application:
    name: peng-code-generation-platform
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/peng_code_generation_platform
    username: root
    password: Mysql88@

server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.peng.codegenerationplatform.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn


